require("yaml-companion").setup({
  builtin_matchers = {
    kubernetes = { enabled = true }
  },
  schemas = {
    -- Add custom schemas for CRDs or other resources if needed
  },
})
require('lspconfig').yamlls.setup(require("yaml-companion").setup_yaml_lsp())

